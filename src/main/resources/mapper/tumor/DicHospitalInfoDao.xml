<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wonders.health.tumor.tumor.dao.DicHospitalInfoDao">
    
	<sql id="DicHospitalInfoColumns">
		a.hospital_id AS "hospitalId",a.hospital_name AS "hospitalName",a.hospital_type AS "hospitalType",a.hospital_level AS "hospitalLevel",a.address_county AS "addressCounty",a.del_flag AS "delFlag",a.source_id AS "sourceId",a.hospital_id_22 AS "hospitalId22"
	</sql>

	<select id="findHospitalsByAreaCode" resultType="com.wonders.health.tumor.tumor.entity.DicHospitalInfo">
		SELECT 
			<include refid="DicHospitalInfoColumns"/>
		FROM CANCER_DIC_HOSPITAL_INFO a
		WHERE
		a.del_flag = '0'
		and  a.HOSPITAL_TYPE='B1'
		<if test="areaCode != null and areaCode != ''">
			AND a.ADDRESS_COUNTY = #{areaCode}
		</if>
	</select>

	<select id="getHospitalById" resultType="com.wonders.health.tumor.tumor.entity.DicHospitalInfo">
		SELECT
		<include refid="DicHospitalInfoColumns"/>
		FROM CANCER_DIC_HOSPITAL_INFO a
		WHERE
		a.del_flag = '0'
		AND a.HOSPITAL_ID = #{orgCode}
	</select>

</mapper>