<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wonders.health.tumor.common.dao.CancerDicDao">

    <sql id="CancerDicColumns">
        a.id AS "id",a.code AS "code",a.name AS "name",a.pingyin AS "pingyin",a.pcode AS "pcode",a.source_id AS "sourceId"
    </sql>

    <select id="getDictsById" resultType="com.wonders.health.tumor.common.entity.CancerDic">
        SELECT
        <include refid="CancerDicColumns"/>
        FROM CANCER_DIC a
        WHERE a.id = #{id}
        AND a.del_flag = '0'
    </select>
</mapper>