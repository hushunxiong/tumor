<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wonders.health.tumor.tumor.dao.LucFamilyCancerHistoryXHDao">
    
	<sql id="LucFamilyCancerHistoryXHColumns">
		a.id AS "id",a.check_id AS "checkId",a.relation AS "relation",a.icd10 AS "icd10",a.cancer_name AS "cancerName",a.age AS "age",a.lived AS "lived",a.del_flag AS "delFlag",a.create_by AS "createBy",a.create_date AS "createDate",a.update_by AS "updateBy",a.update_date AS "updateDate"
	</sql>

	<select id="get" resultType="com.wonders.health.tumor.tumor.entity.LucFamilyCancerHistoryXH">
		SELECT 
			<include refid="LucFamilyCancerHistoryXHColumns"/>
		FROM luc_family_cancer_history_XH a
		WHERE a.id = #{id}
	</select>

	<select id="findAll" resultType="com.wonders.health.tumor.tumor.entity.LucFamilyCancerHistoryXH">
		SELECT 
			<include refid="LucFamilyCancerHistoryXHColumns"/>
		FROM luc_family_cancer_history_XH a
		<where>
			a.del_flag = '0'
		</where>		
			ORDER BY a.update_date DESC
	</select>
	<!-- 分页模块 -->
	<sql id="pageFromAndWhere">
      FROM luc_family_cancer_history_XH a
      <where>
		a.del_flag = '0'
	  </where>
    </sql>

    <select id="pageCount" resultType="java.lang.Integer"
            parameterType="com.wonders.health.tumor.common.model.DataGridSearch">
        select count(1)
        <include refid="pageFromAndWhere"/>
    </select>


    <select id="pageList" resultType="com.wonders.health.tumor.tumor.entity.LucFamilyCancerHistoryXH"
            parameterType="com.wonders.health.tumor.common.model.DataGridSearch">
		<choose>
			<when test="startRecord == 0">
		select * from (
			</when>
			<otherwise>
		select * from ( select row_.*, rownum rownum_ from (
			</otherwise>
		</choose>
        select
			<include refid="LucFamilyCancerHistoryXHColumns"/>
        <include refid="pageFromAndWhere"/>
		<choose>
			<when test="sortField != null and sortField != '' and sortOrder != '' and sortOrder != ''">
				ORDER BY ${sortField} ${sortOrder}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
		<choose>
			<when test="startRecord == 0">
		) row_ where rownum &lt;= #{pageSize}
			</when>
			<otherwise>
		) row_ where rownum &lt;= #{endRecord}) where rownum_ &gt; #{startRecord}
			</otherwise>
		</choose>
    </select>
	
	<insert id="insert">
		INSERT INTO luc_family_cancer_history_XH(
		id,check_id,relation,icd10,cancer_name,age,lived,del_flag,create_by,create_date
		) VALUES (
		#{id},#{checkId},#{relation},#{icd10},#{cancerName},#{age},#{lived},'0',#{createBy},SYSDATE
		)
	</insert>
	
	<update id="update">
		UPDATE luc_family_cancer_history_XH SET
		check_id = #{checkId},relation = #{relation},icd10 = #{icd10},
		cancer_name = #{cancerName},age = #{age},lived = #{lived},
		update_by = #{updateBy},update_date =SYSDATE

		WHERE id = #{id}
	</update>

	<update id="delete">
	    UPDATE luc_family_cancer_history_XH SET del_flag = '1'
	    WHERE id = #{id}
	</update>

	
</mapper>