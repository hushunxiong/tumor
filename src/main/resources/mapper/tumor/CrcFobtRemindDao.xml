<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wonders.health.tumor.tumor.dao.CrcFobtRemindDao">
    
	<sql id="CrcFobtRemindColumns">
		a.id AS "id",a.crc_check_id AS "crcCheckId",a.check_year AS "checkYear",a.first_fobt_remind_date AS "firstFobtRemindDate",a.first_fobt_remind_type AS "firstFobtRemindType",a.second_fobt_remind_date AS "secondFobtRemindDate",a.second_fobt_remind_type AS "secondFobtRemindType",a.per_fobt_remind_date AS "perFobtRemindDate",a.fobt_remind_status AS "fobtRemindStatus",a.del_flag AS "delFlag",a.create_by AS "createBy",a.create_date AS "createDate"
	</sql>

	<select id="get" resultType="com.wonders.health.tumor.tumor.entity.CrcFobtRemind">
		SELECT 
			<include refid="CrcFobtRemindColumns"/>
		FROM crc_fobt_remind a
		WHERE a.id = #{id}
	</select>

	<select id="getByCheckId" resultType="com.wonders.health.tumor.tumor.entity.CrcFobtRemind">
		SELECT
		<include refid="CrcFobtRemindColumns"/>
		FROM crc_fobt_remind a
		WHERE a.crc_check_id = #{checkid}
	</select>

	<select id="findAll" resultType="com.wonders.health.tumor.tumor.entity.CrcFobtRemind">
		SELECT 
			<include refid="CrcFobtRemindColumns"/>
		FROM crc_fobt_remind a
		<where>
			a.del_flag = '0'
		</where>		
	</select>
	<!-- 分页模块 -->
	<sql id="pageFromAndWhere">
      FROM crc_fobt_remind a
      <where>
		a.del_flag = '0'
	  </where>
    </sql>

    <select id="pageCount" resultType="java.lang.Integer"
            parameterType="com.wonders.health.tumor.common.model.DataGridSearch">
        select count(1)
        <include refid="pageFromAndWhere"/>
    </select>


    <select id="pageList" resultType="com.wonders.health.tumor.tumor.entity.CrcFobtRemind"
            parameterType="com.wonders.health.tumor.common.model.DataGridSearch">
		<choose>
			<when test="startRecord == 0">
		select * from (
			</when>
			<otherwise>
		select * from ( select row_.*, rownum rownum_ from (
			</otherwise>
		</choose>
        select
			<include refid="CrcFobtRemindColumns"/>
        <include refid="pageFromAndWhere"/>
		<choose>
			<when test="sortField != null and sortField != '' and sortOrder != '' and sortOrder != ''">
				ORDER BY ${sortField} ${sortOrder}
			</when>
			<otherwise>
			</otherwise>
		</choose>
		<choose>
			<when test="startRecord == 0">
		) row_ where rownum &lt;= #{pageSize}
			</when>
			<otherwise>
		) row_ where rownum &lt;= #{endRecord}) where rownum_ &gt; #{startRecord}
			</otherwise>
		</choose>
    </select>
	
	<insert id="insert">
		INSERT INTO crc_fobt_remind(
		id,crc_check_id,check_year,first_fobt_remind_date,first_fobt_remind_type,second_fobt_remind_date,second_fobt_remind_type,per_fobt_remind_date,fobt_remind_status,del_flag,create_by,create_date
		) VALUES (
		#{id},#{crcCheckId},#{checkYear},#{firstFobtRemindDate},#{firstFobtRemindType},#{secondFobtRemindDate},#{secondFobtRemindType},#{perFobtRemindDate},#{fobtRemindStatus},#{delFlag},#{createBy},#{createDate}
		)
	</insert>
	
	<update id="update">
		UPDATE crc_fobt_remind SET
		id = #{id},crc_check_id = #{crcCheckId},check_year = #{checkYear},first_fobt_remind_date = #{firstFobtRemindDate},first_fobt_remind_type = #{firstFobtRemindType},second_fobt_remind_date = #{secondFobtRemindDate},second_fobt_remind_type = #{secondFobtRemindType},per_fobt_remind_date = #{perFobtRemindDate},fobt_remind_status = #{fobtRemindStatus},del_flag = #{delFlag},create_by = #{createBy},create_date = #{createDate}
		WHERE id = #{id}
	</update>

	<update id="delete">
	    UPDATE crc_fobt_remind SET del_flag = '1'
	    WHERE id = #{id}
	</update>

	
</mapper>